# Lexical Entries

One file per word/lemma, filename = its normalized lemma (lowercase,
diacritics preserved) as the slug. Populated by the build pipeline
(`okfbuild/pipeline.py`, added in section-05-pipeline) — empty until the
first pipeline run (section-07-pilot).
