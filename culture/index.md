# Cultural Context

One file per person, theme, or work, filename = a short kebab-case slug
(e.g. `cavafy.md`). Populated by the build pipeline (`build/pipeline.py`,
added in section-05-pipeline) — empty until the first pipeline run
(section-07-pilot).
