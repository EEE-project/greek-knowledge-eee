# Grammatical Rules

One file per documented grammatical rule -- a synchronic paradigm/syntax
rule scoped to one period, or a period-to-period change. Filename = a
short kebab-case slug (e.g. `aorist-3pl-osan.md`). Populated by the
build pipeline (`okfbuild/pipeline.py`, added in section-05-pipeline).
