# Grammatical Rules

One file per documented period-to-period morphological pattern, filename
= a short kebab-case slug (e.g. `aorist-3pl-osan.md`). Populated by the
build pipeline (`okfbuild/pipeline.py`, added in section-05-pipeline) —
empty until the first pipeline run (section-07-pilot).
